DEBUG = True
SECRET_KEY = "change-me-to-a-random-secret-key"
ALLOWED_HOSTS = ["localhost", "127.0.0.1"]
CORS_ALLOWED_ORIGINS = [
    "http://localhost:4200",
]

# Gemini API key – get one at https://aistudio.google.com/app/apikey
# You can also supply this via the GEMINI_API_KEY environment variable instead.
GEMINI_API_KEY = ""

# URL of the Angular shell HTML used by the OG-meta shim.
# The default points to the "client" Docker service (production).
# Uncomment and adjust for local development:
# CLIENT_SHELL_URL = "http://localhost:4200/index.html"
